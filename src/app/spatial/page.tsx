"use client";
import SpatialCanvas from '@/components/demo/SpatialCanvas';

export default function SpatialPage() {
    return (
        <main className="min-h-screen bg-black">
            <SpatialCanvas />
        </main>
    );
}
